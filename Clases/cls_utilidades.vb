Imports Newtonsoft.Json.Linq

Public Class cls_utilidades
    'Public Function FG_VALIDA_PARAMETRO(ByRef item As JObject, ByRef name As String, Optional ByVal int As Boolean = False) As String
    '    Dim resp As String
    '    If item.ContainsKey(name) Then
    '        If Not IsDBNull(item.GetValue(name)) Then
    '            If item.GetValue(name) IsNot Nothing Then
    '                If item.GetValue(name) <> "" Then
    '                    resp = item.GetValue(name).ToString
    '                    Return resp
    '                End If
    '            End If
    '        End If
    '    End If
    '    resp = IIf(int = True, 0, "")
    '    Return resp
    'End Function

End Class
